apply plugin: 'war'

dependencies {
    runtime project(':genie-server')
    runtime ('com.netflix.karyon:karyon-admin-web:1.0.27') {
        exclude group: 'commons-collections', module: 'commons-collections'
    }
}